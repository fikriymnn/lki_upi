/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com','images.pexels.com',"masuk-ptn.com","fpmipa1.upi.edu" ],
    },
}

module.exports = nextConfig
